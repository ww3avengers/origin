{
  "installCommand": "pnpm -r i --frozen-lockfile",
  "buildCommand": "pnpm -w run frontend:ci",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ],
  "ignoreCommand": "echo 'Use monorepo build via pnpm workspaces'"
}
